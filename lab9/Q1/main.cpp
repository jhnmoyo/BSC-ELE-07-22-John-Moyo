#include <iostream>
#include "Rectangle.h"
#include "Triangle.h"

using namespace std;
int main() {
    Rectangle react;
    Triangle trgl;
    rect.SetValues(4,5);
    trgl.SetValues(4,5);
    
    cout <<react.Area() << `\n`;
    cout <<trgl.Area() << `\n`;
}